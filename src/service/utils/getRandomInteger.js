// @ts-check

'use strict';

/**
 * Возвращает случайное число
 * @param {number} min - минимальный предел числа.
 * @param {number} max - максимальный предел числа.
 *  @return {number} - случайное число.
 */
const getRandomInteger = (min, max) => {

};

module.exports = {
  getRandomInteger
};
